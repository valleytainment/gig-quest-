# `components/feedback/` — Feedback States

> Layer: 🟧 UI | Used by admin, artist, and loading routes

| Component | Responsibility |
|-----------|----------------|
| `EliteLoader.tsx` | Branded spinner / loading indicator |
| `EmptyState.tsx` | Empty list / no data placeholder |
| `ErrorState.tsx` | Recoverable error display |
| `PageSkeleton.tsx` | Route-level loading skeleton |

**Rule:** No business logic or Firestore access.
