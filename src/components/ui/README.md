# `components/ui/` — shadcn/ui Primitives

> Layer: 🟧 UI | Generated via shadcn — **do not add elite blocks to each file**

| Component | Use |
|-----------|-----|
| `button`, `input`, `label` | Forms |
| `card`, `badge`, `separator` | Layout |
| `dialog`, `tabs`, `scroll-area` | Overlays / navigation |
| `table`, `avatar` | Admin/artist data display |
| `sonner` | Toast notifications |

Config: `components.json` at repo root.

**Rule:** Style via design tokens in `src/styles/`; avoid one-off overrides in primitives.
